[tool.poetry]
name = "random-filters"
version = "0.3.1"
description = ""
authors = ["Renan <renancavalcantercb@protonmail.com>"]
readme = "README.md"
packages = [{include = "random_filters", from = "src"}, {include = "random_date", from = "src"}]

[tool.poetry.dependencies]
python = "^3.10"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
