[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "micropsi_challenge"
version="1.0.0"
description = "Package to prove my skills"
readme = "README.md"
license = {file = "LICENSE.txt"}
requires-python = ">=3.9"
keywords = ["micropsi", "challenge"]

[project.optional-dependencies]
dev = ["pytest"]