[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "kagepy"
version = "0.0.1"
dependencies = [

]

[tool.setuptools.packages.find]
where = ["src"]

[project.scripts]
pykage = "kagepy.main:main"
