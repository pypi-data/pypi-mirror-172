[project]
name = "Bedtime"
version = "0.0.2"
readme = "README.md"
requires-python = ">=3.8"
license = { file="LICENSE" }
authors = [{name="dunkyl"}]
dependencies = [
    'pywin32; sys_platform == "win32"',
    'pyobjc; sys_platform == "darwin"'
]
[project.urls]
repository = "https://github.com/dunkyl/bedtime"

[build-system]
requires = ["setuptools", "wheel"]