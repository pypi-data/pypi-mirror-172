[metadata]
name = lambda-thread-pool
version = 0.0.2
author = Aliaksei Kankou
author_email = aliaksei.kankou@gmail.com
description = AWS Lambda thread pool (lambda-thread-pool) uses multiprocessing.Pipe instead of multiprocessing.Queue. It provides the ability to perform parallel execution within the AWS lambda Python execution environment.
long_description = file: README.md
long_description_content_type = text/markdown
url = https://github.com/kankou-aliaksei/lambda-thread-pool
project_urls = 
	Bug Tracker = https://github.com/kankou-aliaksei/lambda-thread-pool/issues

[options]
package_dir = 
	= src
packages = find:
python_requires = >=3.7

[options.packages.find]
where = src

[egg_info]
tag_build = 
tag_date = 0

