[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "sphinx_tagtoctree"
authors = [{name = "Half Scheidl", email = "noreply@hscheidl.com"}]
readme = "README.md"
license = {file = "LICENSE"}

classifiers = ["License :: OSI Approved :: MIT License"]
dynamic = ["version", "description"]
dependencies = [
    "sphinx>=1.7.9"
]

[project.urls]
Home = "https://github.com/haschdl/sphinx-tagtoctree"

