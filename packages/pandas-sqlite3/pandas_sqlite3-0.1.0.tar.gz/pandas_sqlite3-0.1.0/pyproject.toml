[tool.poetry]
name = "pandas-sqlite3"
version = "0.1.0"
description = ""
authors = ["hamilton <hamilton@pattern.com>"]
readme = "README.md"
packages = [{include = "pandas_sqlite3"}]

[tool.poetry.dependencies]
python = ">=3.8"
pandas = "^1.5.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
