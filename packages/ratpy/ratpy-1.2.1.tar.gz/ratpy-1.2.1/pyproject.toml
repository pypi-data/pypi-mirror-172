[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = 'ratpy'
authors = [{name='Steve Ayrton', email='stephen_ayrton@waters.com'}]
dynamic = ['version','description']
