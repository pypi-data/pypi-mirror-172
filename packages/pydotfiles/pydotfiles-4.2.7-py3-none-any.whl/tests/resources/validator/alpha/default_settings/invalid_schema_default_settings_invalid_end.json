{
    "version": "alpha",
    "schema": "default_settings",
    "default_settings": [
        {
            "name": "invalid minimal default setting example",
            "command": "echo 'invalid minimal default setings example'",
            "start": "some random invalid string",
            "end": "some random invalid string"
        }
    ]
}
