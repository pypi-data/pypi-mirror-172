{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "alpha.common.json",
    "properties": {
        "version": {
            "enum": ["alpha"]
        },
        "schema": {
            "enum": ["core", "default_settings", "developer_environments", "manifest"]
        }
    },
    "required": ["version", "schema"]
}
