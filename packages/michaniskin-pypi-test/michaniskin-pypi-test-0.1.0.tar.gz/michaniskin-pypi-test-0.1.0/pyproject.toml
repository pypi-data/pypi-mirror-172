[tool.poetry]
name = "michaniskin-pypi-test"
version = "0.1.0"
description = ""
authors = ["Micha Niskin <micha.niskin@gmail.com>"]
readme = "README.md"

[tool.poetry.scripts]
pypi-test = "michaniskin_pypi_test.main:app"

[tool.poetry.dependencies]
python = "^3.9"
typer = {extras = ["all"], version = "^0.6.1"}

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
