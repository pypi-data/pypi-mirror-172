[tool.poetry]
name = "aslabs-dependencies-firestore"
version = "0.0.3"
description = ""
authors = ["Titusz Ban <tituszban@antisociallabs.io>"]
readme = "README.md"
packages = [
    { include = "aslabs" }
]

[tool.poetry.dependencies]
python = "^3.9"
aslabs-dependencies = "^0.0.2"
firebase-admin = "^5.2.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
