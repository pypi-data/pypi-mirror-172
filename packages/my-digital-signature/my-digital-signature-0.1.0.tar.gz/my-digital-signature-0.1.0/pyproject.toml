[tool.poetry]
name = "my-digital-signature"
version = "0.1.0"
description = ""
authors = ["ConstantZero <mrzero@duck.com>"]
readme = "README.md"
license = "GPL-3.0-or-later"
packages = [{include = "my_digital_signature"}]

[tool.poetry.dependencies]
python = "^3.8"
num2words = "^0.5.12"

[tool.poetry.scripts]
signature = "my_digital_signature.SIGNATURE:create_signature"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
